#!/usr/bin/env ruby

ENV['BUNDLE_GEMFILE'] ||= "#{ENV['cw_ROOT']}/opt/forge/Gemfile"
$: << "#{ENV['cw_ROOT']}/opt/forge/lib"

require 'bundler/setup'
require 'alces/forge/cli'

Alces::Forge::CLI.new.run
